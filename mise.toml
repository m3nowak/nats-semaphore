[tools]
pdm = "2"
python = "3.10"

[tasks.pdm-venv]
run = "test -d .venv || pdm venv create {{tools.python.path}}"
run_windows = "if not exist .venv\\NUL pdm venv create {{tools.python.path}}"

[tasks.pdm-install]
run = "pdm install"
wait_for = ["pdm-venv"]

[tasks.setup]
depends = ["pdm-venv", "pdm-install"]

[tasks.test]
run = "pdm run pytest"
